<h1>Edit the Hospital</h1>

<h4><%= link_to "Show", hospital_path(@hospital) %>
<%# <%= javascript_pack_tag 'application' %>


<%= form_with model: @hospital do |form| %>
  <div>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :address %><br>
    <%= form.text_field :address %>
  </div>

  <div>
    <%= form.label :city %><br>
    <%= form.text_field :city %>
  </div>

  <div>
    <%= form.label :state %><br>
    <%= form.text_field :state %>
  </div>

  <div>
    <%= form.label :country %><br>
    <%= form.text_field :country %>
  </div>

  <div>
    <%= form.label :phone_number %><br>
    <%= form.text_field :phone_number %>
  </div>

  <div>
    <%= form.label :email_address %><br>
    <%= form.text_field :email_address %>
  </div>

  <div>
    <%= form.label :emergency_contact %><br>
    <%= form.text_field :emergency_contact %>
  </div>

  <div>
    <%= form.label :capacity %><br>
    <%= form.number_field :capacity %>
  </div>

  <div>
    <%= form.label :specialties %><br>
    <%= form.text_field :specialties %>
  </div>

  <div>
    <%= form.label :operating_hours %><br>
    <%= form.text_field :operating_hours %>
  </div>

  <div>
    <%= form.label :insurance_accepted %><br>
    <%= form.text_field :insurance_accepted %>
  </div>

  <div>
    <%= form.label :website %><br>
    <%= form.text_field :website %>
  </div>

  <div>
    <%= form.hidden_field :latitude, id: 'latitude' %>
    <%= form.hidden_field :longitude, id: 'longitude' %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

<%# <script>
  document.addEventListener("DOMContentLoaded", function() {
    if ("geolocation" in navigator) {
      navigator.geolocation.getCurrentPosition(
        function(position) {
          const latitude = position.coords.latitude;
          const longitude = position.coords.longitude;

          // Ensure these elements exist in the DOM
          const latitudeInput = document.getElementById('latitude');
          const longitudeInput = document.getElementById('longitude');

          if (latitudeInput && longitudeInput) {
            latitudeInput.value = latitude;
            longitudeInput.value = longitude;
          } else {
            console.error("Latitude or Longitude input element not found.");
          }
        },
        function(error) {
          console.error("Error:", error.message);
        }
      );
    } else {
      console.log("Geolocation API is not supported.");
    }
  });
</script> %>